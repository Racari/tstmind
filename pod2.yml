apiVersion: v1
kind: Pod                                            
metadata:
  name: app-test2     # Единственное изменение
spec:                                                
  containers:
    - image: rinormaloku/sentiment-analysis-frontend 
      name: app-test2                            
      ports:
        - containerPort: 80
