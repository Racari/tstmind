apiVersion: v1
kind: Pod                                            
metadata:
  name: app-test4     # Единственное изменение
spec:                                                
  containers:
    - image: rinormaloku/sentiment-analysis-frontend 
      name: app-test                            
      ports:
        - containerPort: 80
